# Reference: MobileViT-S (Mehta & Rastegari, ICLR 2022)
# Architecture combination: encoder=timm_mobilevit_s + decoder=bilinear
# 架构组合：编码器=timm_mobilevit_s + 解码器=双线性

model:
  num_classes: 2
  img_size: 224

  encoder:
    name: timm_mobilevit_s
    pretrained: true
    in_channels: 3
    params: {}

  decoder:
    name: bilinear
    params: {}

  skip_connection:
    name: concat
    params: {}

  bottleneck:
    name: none

data:
  type: generic
  img_size: 224
  train_dir: ./data/Binary/train
  test_dir: ./data/Binary/test
  train_list: ./data/Binary/lists/train.txt
  test_list: ./data/Binary/lists/test.txt

training:
  epochs: 300
  batch_size: 24
  num_workers: 4

  loss:
    name: compound
    params:
      losses:
        - name: ce
          weight: 0.4
        - name: dice
          weight: 0.6

  optimizer:
    name: adamw
    lr: 0.001
    weight_decay: 0.0001

  scheduler:
    name: cosine
    min_lr: 0.000001
